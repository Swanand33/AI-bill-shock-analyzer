[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Coverage settings
addopts =
    -v
    --cov=utils
    --cov-report=term-missing
    --cov-report=html

# Ignore warnings
filterwarnings =
    ignore::DeprecationWarning
